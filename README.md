# MapLog

一个轻量级的 C++ 日志库，支持多种日志级别、文件轮转和自定义格式。

## 特性

- 支持多个日志级别（DEBUG, INFO, WARN, ERROR, FATAL）
- 支持文件和控制台输出
- 支持日志文件轮转（大小限制和数量限制）
- 支持日志格式自定义
- 线程安全
- JSON 配置支持
- 支持流式语法和格式化语法
- 支持条件日志

## 使用方法

### 基本使用